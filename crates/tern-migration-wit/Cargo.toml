[package]
name = "tern-migration-wit"
version = "0.1.0"
edition = "2024"
description = "WIT interface definitions for Tern migration components"
license = "MIT"

[dependencies]
# xxhash3 for fast, high-quality schema checksums
# Pure Rust implementation works on both native and wasm32 targets
xxhash-rust = { version = "0.8", features = ["xxh3"] }
