pub mod cli;

#[cfg(test)]
mod tests {
    #[test]
    fn smoke_test() {
        // This test exists to ensure the test infrastructure works
        // before any real code is written.
        assert!(true);
    }
}
